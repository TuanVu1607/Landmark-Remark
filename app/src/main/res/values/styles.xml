<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App Theme-->
    <style name="Theme.LandmarkRemark" parent="Base.Theme.LandmarkRemark">
        <!-- Kích thước phông chữ mặc định, kích thước phông chữ của Nút, TextView -->
        <item name="android:textSize">@dimen/base_text_size</item>
    </style>

    <!-- Splash Screen Theme-->
    <style name="SplashScreenTheme" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <item name="postSplashScreenTheme">@style/Theme.LandmarkRemark</item>
    </style>
</resources>